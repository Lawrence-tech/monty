#include "monty.h"

/**
  *order_stack - sets the format for the data to a LIFO stack.
  *@head: pointer to the head of list
  *@l_num:the new line command
  */
void order_stack(stack_t **head, unsigned int l_num)
{
	 (void)head;
	 (void)l_num;

	 vars.order = 1;
}
